[package]
name = "nbcad-mcp-bus"
version = "0.1.0"
edition = "2021"
description = "Transport-agnostic MCP request/reply bus (in-memory required; NATS/Kafka/MQTT-shaped subjects)"
license = "LGPL-2.0-or-later"
repository = "https://github.com/jackControls/noBS-CAD"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
nbcad-id = { path = "../id" }

[dev-dependencies]
