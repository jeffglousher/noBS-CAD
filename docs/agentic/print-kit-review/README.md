# Print-kit review shots (2026-08-17)

Radial-axis thin thrust. Pack height is the roller diameter (exam Ø8 × L9.6
on PCD 44). `pack` hides the rotor so the lying rollers / radial windows
are visible. `races` looks up from under the plate. `hub` is a close stack.
`side` shows the short pack under the plate; the square stator post is
supposed to reach the retainer — that is not a drum.

These are the check renders from `npm run render:print-kit`, copied here so they are not stuck in gitignored `docs/qa/`.
