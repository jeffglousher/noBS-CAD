# Print-kit review shots (2026-08-17)

Radial-axis thin thrust **under the blade roots**. Exam: 6× Ø8.0 × L9.6
on PCD 58.1 (outer land r=33.8, blade R=34.0). Cage 74.1 × 8.0; flange
Ø74.1 inside plate Ø76.0. Cage ID looser than the plate bore. Base
Y-frame 52×58, not a race-OD cylinder. `pack` hides the rotor so the
lying rollers / radial windows are visible. `races` looks up from under
the plate. `hub` is a close stack. `side` shows the short pack; the
square stator post is supposed to reach the retainer — that is not a
drum.

These are the check renders from `npm run render:print-kit`, copied here so they are not stuck in gitignored `docs/qa/`.
