# Print-kit review shots (2026-08-17)

Radial-axis thin thrust **under the blade roots**. Pack height is the
roller diameter. The cage is a spacer (ID looser than the plate bore).
`pack` hides the rotor so the lying rollers / radial windows are
visible. `races` looks up from under the plate. `hub` is a close stack.
`side` shows the short pack; the square stator post is supposed to
reach the retainer — that is not a drum. The base boss seats the axle
and does not match the race OD.

These are the check renders from `npm run render:print-kit`, copied here so they are not stuck in gitignored `docs/qa/`.
