# Print-kit review shots (2026-08-17)

Tune pass: keeper walls so rollers cannot slide out; thinner Y-frame
and root plate; organic airfoil roots; short tip taper to a flat
landing. Clip CAD is unchanged. Merged stator: thin Y-frame + race
ring + keepers + open top-load fence + short D-journal. Radial-axis
thin thrust **under the blade roots**. Rollers print standing and drop
in from above (not PIP). Clocked C-clip snaps into a journal groove
and does not rub the rotor. The plate drops over a constant journal;
the clip goes on last (`pack` still shows it seated). `pack` hides
the rotor so the lying rollers / top-load windows are visible. `races`
looks up from under the plate. `hub` is a close stack (plate h4).
`side` shows the short pack — not a drum, not two flats.

These are the check renders from `npm run render:print-kit`, copied here so they are not stuck in gitignored `docs/qa/`.
